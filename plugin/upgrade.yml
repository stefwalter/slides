- hosts: all
  name: upgrade
  gather_facts: no
  tasks:
    - name: upgrade host
      command: 'rpm-ostree upgrade'
    - name: restart host
      shell: sleep 2 && shutdown -r now
      async: 1
      poll: 0
      ignore_errors: true
